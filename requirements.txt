# 项目依赖（pip install -r requirements.txt）
# ⚠️ 需要 Python 3.10–3.13；推荐 3.12（统一 venv；勿用 3.14）
# 安装：.\venv\Scripts\pip install -r requirements.txt -i https://pypi.org/simple

# ── 核心依赖（必须安装）────────────────────────────
elasticsearch>=8.0.0,<9.0.0            # Elasticsearch 8.x 客户端
httpx>=0.27.0                           # HTTP 客户端（LLM API / Tavily 流式调用）
requests>=2.31.0                        # HTTP 客户端（Jina Embedding API）
langchain-text-splitters>=0.0.1        # RecursiveCharacterTextSplitter 文本分块
tiktoken>=0.5.0                         # Token 计数（langchain 需要）
pdfplumber>=0.10.0                      # PDF 文本提取（MinerU 降级用）
python-dotenv>=1.0.0                    # 读取 .env 文件

# ── Vue3 前端配套 API（FastAPI）────────────────────
fastapi>=0.110.0                        # REST / SSE API
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
uvicorn[standard]>=0.27.0               # ASGI 服务器
pydantic>=2.0.0                         # 请求/响应校验（FastAPI 依赖）
python-multipart>=0.0.9                 # 文件上传
python-docx>=1.1.0                      # DOCX 结构解析（MinerU 降级用）

# ── MinerU 文档解析（统一装在同一 venv，pipeline/CPU）──
# 体积大（含 torch 等）；单独进程启动：mineru-api / mineru_service
mineru[pipeline]==3.4.4

# ── 可选依赖（按需安装）────────────────────────────
# pypdfium2>=4.20.0                     # PDF 渲染（扫描件 OCR 用）
# rapidocr>=3.0.0                       # 中文 OCR 识别
# numpy>=1.24.0                          # 图像处理（rapidocr 需要）
